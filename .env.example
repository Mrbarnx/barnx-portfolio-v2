# Used for canonical links, sitemap entries and social metadata.
# Replace this with the custom domain when it is connected.
NEXT_PUBLIC_SITE_URL=https://barnx-portfolio-v2.vercel.app

# Phase 3 will use these for Supabase Auth and database reads.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Never expose a Supabase service-role key through a NEXT_PUBLIC_ variable.
