import { notFound } from 'next/navigation';
import Link from 'next/link';
import { getPublishedStudioResource } from '@/lib/cms/publicStudio';

export const dynamic = 'force-dynamic';

export default async function ResourceDetail({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const r=await getPublishedStudioResource(slug);if(!r)notFound();const action=r.download||r.externalUrl;return <main className="page resourceDetail"><Link className="back" href="/barnx-studio">← Barnx Studio</Link><section className="resourceHero"><div className="resourceIcon huge">{r.icon}</div><span className="eyebrow">{r.type} · {r.free?'FREE':'PREMIUM'}</span><h1>{r.title}</h1><p>{r.description}</p>{action?<a className="button black" download={Boolean(r.download)} href={action} target={r.externalUrl?'_blank':undefined} rel={r.externalUrl?'noreferrer':undefined}>{r.download?'Download resource ↓':'Open resource ↗'}</a>:<a className="button black" href="mailto:mrbarnx@gmail.com?subject=Barnx%20Studio%20Resource">Request access ↗</a>}</section><section className="caseColumns"><div><span className="eyebrow">WHAT'S INCLUDED</span><ul>{r.includes.map(x=><li key={x}>{x}</li>)}</ul></div><div><span className="eyebrow">BEST FOR</span><p>{r.bestFor}</p><div className="tags large">{r.tech.map(t=><b key={t}>{t}</b>)}</div></div></section><section className="nextCase"><p>Keep exploring</p><Link href="/barnx-studio">Browse all resources →</Link></section></main>}
