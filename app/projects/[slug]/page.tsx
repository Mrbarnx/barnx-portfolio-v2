import { notFound } from 'next/navigation';
import Link from 'next/link';
import { projects } from '@/data/content';
export function generateStaticParams(){return projects.map(p=>({slug:p.slug}))}
export default async function ProjectDetail({params}:{params:Promise<{slug:string}>}){const {slug}=await params; const p=projects.find(x=>x.slug===slug); if(!p)notFound();return <main className="page caseStudy"><Link className="back" href="/projects">← All projects</Link><section className="caseHero"><span className="eyebrow">{p.category}</span><h1>{p.title}</h1><p>{p.overview}</p><div className="caseLinks">{p.live&&<a href={p.live} target="_blank">Live website ↗</a>}{p.github&&<a href={p.github} target="_blank">GitHub ↗</a>}</div></section><div className={`caseMock ${p.tone}`}><div className="browser"><i/><i/><i/></div><strong>{p.display}</strong><small>{p.visualSubtitle}</small></div><section className="caseColumns"><div><span className="eyebrow">THE PROBLEM</span><h2>{p.problem}</h2></div><div><span className="eyebrow">THE SOLUTION</span><p>{p.solution}</p></div></section><section className="caseSection"><span className="eyebrow">KEY FEATURES</span><div className="featureGrid">{p.features.map(f=><article key={f}><span>✦</span><p>{f}</p></article>)}</div></section><section className="caseColumns"><div><span className="eyebrow">MY ROLE</span><h2>{p.role}</h2></div><div><span className="eyebrow">TECHNOLOGY</span><div className="tags large">{p.tech.map(t=><b key={t}>{t}</b>)}</div></div></section><section className="caseColumns"><div><span className="eyebrow">CHALLENGES</span><p>{p.challenges}</p></div><div><span className="eyebrow">LESSONS</span><p>{p.lessons}</p></div></section><section className="nextCase"><p>Explore more work</p><Link href="/projects">View all projects →</Link></section></main>}
