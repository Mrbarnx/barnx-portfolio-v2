import Link from 'next/link';import {requireCmsAdmin} from '@/lib/admin/requireCmsAdmin';import {StudioResourceForm} from '../../StudioResourceForm';import styles from '../../../content.module.css';
export default async function NewStudioResource(){const {supabase}=await requireCmsAdmin();const {data:categories}=await supabase.from('studio_categories').select('id,title').order('sort_order');return <main className={styles.page}><div className={styles.wrap}><Link className={styles.back} href="/admin/studio">← Studio CMS</Link><header className={styles.header}><div><p className={styles.eyebrow}>New resource</p><h1>Add Studio resource</h1><p>Choose its public category, then save as a draft until it is ready.</p></div></header><StudioResourceForm categories={categories??[]}/></div></main>}
