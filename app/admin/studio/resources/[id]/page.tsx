import Link from 'next/link';import {notFound} from 'next/navigation';import {requireCmsAdmin} from '@/lib/admin/requireCmsAdmin';import {StudioResourceForm} from '../../StudioResourceForm';import styles from '../../../content.module.css';
export default async function EditStudioResource({params,searchParams}:{params:Promise<{id:string}>;searchParams:Promise<{saved?:string}>}){const {id}=await params;const {supabase}=await requireCmsAdmin();const {data}=await supabase.from('studio_resources').select('*').eq('id',id).maybeSingle();if(!data)notFound();const {saved}=await searchParams;return <main className={styles.page}><div className={styles.wrap}><Link className={styles.back} href="/admin/studio">← Studio CMS</Link><header className={styles.header}><div><p className={styles.eyebrow}>Edit resource</p><h1>{data.title}</h1></div></header>{saved?<p className={styles.notice}>Saved successfully as {saved}.</p>:null}<StudioResourceForm row={data}/></div></main>}
