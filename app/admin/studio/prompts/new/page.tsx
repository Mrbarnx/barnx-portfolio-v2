import Link from 'next/link';import {requireCmsAdmin} from '@/lib/admin/requireCmsAdmin';import {PromptResourceForm} from '../../PromptResourceForm';import styles from '../../../content.module.css';
export default async function NewPrompt(){await requireCmsAdmin();return <main className={styles.page}><div className={styles.wrap}><Link className={styles.back} href="/admin/studio">← Studio CMS</Link><header className={styles.header}><div><p className={styles.eyebrow}>New prompt</p><h1>Add prompt</h1><p>The full prompt lives in Supabase, so future entries need no code file.</p></div></header><PromptResourceForm/></div></main>}
