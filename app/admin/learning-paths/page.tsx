import Link from 'next/link';import {requireCmsAdmin} from '@/lib/admin/requireCmsAdmin';import styles from '../content.module.css';
export const dynamic='force-dynamic';
export default async function LearningAdmin({searchParams}:{searchParams:Promise<{error?:string}>}){const {supabase}=await requireCmsAdmin();const {data}=await supabase.from('learning_paths').select('id,slug,title,difficulty,estimated_duration,published,updated_at').order('updated_at',{ascending:false});const {error}=await searchParams;return <main className={styles.page}><div className={styles.wrap}><Link className={styles.back} href="/admin">← Admin home</Link><header className={styles.header}><div><p className={styles.eyebrow}>Phase 6</p><h1>Learning Paths</h1><p>Create paths, then organize their published modules and lessons.</p></div><Link className={styles.button} href="/admin/learning-paths/new">Add learning path</Link></header>{error?<p className={styles.error}>Check the required fields and try again.</p>:null}<div className={styles.grid}>{data?.map(path=><article className={styles.card} key={path.id}><div className={styles.badges}><span className={path.published?styles.live:''}>{path.published?'Published':'Draft'}</span><span>{path.difficulty||'Unspecified'}</span></div><h2>{path.title}</h2><p>{path.estimated_duration||'Self-paced'}</p><small>/{path.slug}</small><div className={styles.actions}><Link className={styles.secondary} href={`/admin/learning-paths/${path.id}`}>Edit curriculum</Link></div></article>)}</div>{!data?.length?<p className={styles.empty}>No CMS learning paths yet. The existing Docker Fundamentals path remains live and unchanged.</p>:null}</div></main>}
