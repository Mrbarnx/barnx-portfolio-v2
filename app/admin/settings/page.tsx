import Link from 'next/link';import {requireCmsAdmin} from '@/lib/admin/requireCmsAdmin';import {defaultSiteSettings} from '@/lib/cms/publicSettings';import {saveSiteSettings} from '../content-actions';import styles from '../content.module.css';
export const dynamic='force-dynamic';
export default async function SettingsAdmin({searchParams}:{searchParams:Promise<{saved?:string;error?:string}>}){const {supabase}=await requireCmsAdmin();const {data}=await supabase.from('site_settings').select('value').eq('key','site.profile').maybeSingle();const value={...defaultSiteSettings,...((data?.value&&typeof data.value==='object')?data.value:{})};const query=await searchParams;return <main className={styles.page}><div className={styles.wrap}><Link className={styles.back} href="/admin">← Admin home</Link><header className={styles.header}><div><p className={styles.eyebrow}>Phase 9</p><h1>Site Settings</h1><p>Edit only the public identity, links and metadata that should change without code.</p></div></header>{query.saved==='true'?<p className={styles.notice}>Public settings saved. The homepage and footer now use them.</p>:null}{query.saved==='failed'||query.error?<p className={styles.error}>Settings were not saved. Check every URL and required field.</p>:null}<p className={styles.guard}>Never place API keys, passwords or private tokens here. This record is intentionally public.</p><form className={styles.form} action={saveSiteSettings}><section className={styles.section}><h2>Homepage identity</h2><div className={styles.stack}><label>Professional headline<textarea name="headline" required rows={4} defaultValue={value.headline}/></label><label>Availability label<input name="availability" required defaultValue={value.availability}/></label><label>Location<input name="location" required defaultValue={value.location}/></label><label>Resume path<input name="resumeUrl" required defaultValue={value.resumeUrl}/></label></div></section><section className={styles.section}><h2>Contact & social links</h2><div className={styles.fields}><label>Email<input name="email" type="email" required defaultValue={value.email}/></label><label>GitHub URL<input name="github" type="url" required defaultValue={value.github}/></label><label>LinkedIn URL<input name="linkedin" type="url" required defaultValue={value.linkedin}/></label><label>X URL<input name="x" type="url" required defaultValue={value.x}/></label><label>TikTok URL<input name="tiktok" type="url" required defaultValue={value.tiktok}/></label></div></section><section className={styles.section}><h2>SEO defaults</h2><div className={styles.stack}><label>Default title<input name="seoTitle" required defaultValue={value.seoTitle}/></label><label>Default description<textarea name="seoDescription" required rows={4} defaultValue={value.seoDescription}/></label></div></section><div className={styles.formActions}><button className={styles.button}>Save settings</button></div></form></div></main>}
